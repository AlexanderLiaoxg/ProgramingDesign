用户登陆界面 ： 可视化；
                监听器
                数据库


连接服务端思维：   
                   ①主界面创造一个socket客户端类，用来连接服务端，
                   ②定义一个类CommandToServer用来传递命令，同步命令属性。
                   ③客户端，死循环判定命令类型，执行相应程序（诺返回结果，发送给服务端）
                   ④服务端，接受命令字符串，判断命令类型，执行相应的数据库类，发送结果给客户端