Psrt 监听为什么监听到俩次

监听器里面的值传不出来

JLIST里面的P.close二次监听不断的叠加，可能存在隐患（在N次切歌后）
里面的Button监听也不断的增加

歌曲列表用的是纯List，构建是参数是String数组，是否改为List为参数更加的好

按钮上一曲下一曲，关于监听器重复参考了JLIST重复问题将其放入，嵌入List中得到了解决

进度条JSlider,添加sleep()延迟获得数据，但是不断的new使得效率低下

搜索会导致正在播放的音乐卡顿或者关闭：  新建立个线程专门搜索（播放搜索歌曲的时候，再度搜索导致播放终断，且无法再度播放）

歌词移动导致JList绘制部分重复糊影

Lrics,死循环线程无法退出，给定判定退出FLAG改为false；

死循环线程中包含sleep，无法中断线程：在sleep前面添加Thread.current.isInterrupt判断，跳出死循环，因为sleep本身是抛出一个interrupt异常

JAVA传递数据无法同步修改：StringBuffer类型数据转换、类对象传递修改属性

线程内容与主线程内容的冲突问题，线程的关闭启动要适当袷宜，之间可以留下一部分空白时间

getlyrics这个类使得程序运行的相当慢

相当的子线程抢占主线程资源，使用上/下曲会出现明显的停顿

做的不完善，个别信息没有会出现错误

我感觉，歌单、歌曲获取写的太LOW了

收藏歌单太LOW